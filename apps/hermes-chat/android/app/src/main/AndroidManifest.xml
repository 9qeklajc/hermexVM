<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Voice message recording (getUserMedia in the WebView). Capacitor's
         BridgeWebChromeClient requests BOTH of these for AUDIO_CAPTURE; an
         undeclared MODIFY_AUDIO_SETTINGS makes it deny the WebView request
         even after the user granted the microphone. -->
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- Android 13+ runtime permission for @capacitor/local-notifications -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Camera photo capture from the attach menu: the capture="environment"
         input makes BridgeWebChromeClient launch ACTION_IMAGE_CAPTURE. When
         CAMERA is declared here, Capacitor requests it at runtime the first
         time the WebView opens the camera, then hands the photo back. -->
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- Android 11+ package visibility: Capacitor's BridgeWebChromeClient
         calls PackageManager.resolveActivity(ACTION_IMAGE_CAPTURE) before
         launching the device camera from the attach menu's capture input.
         Without this query declaration that lookup returns null on
         targetSdk 30+, so the camera silently falls back to the document
         picker ("Media capture intent could not be launched"). -->
    <queries>
        <intent>
            <action android:name="android.media.action.IMAGE_CAPTURE" />
        </intent>
    </queries>
    <!-- Keep the app installable on camera-less devices despite CAMERA. -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
</manifest>
